# 为 Slowday 贡献

感谢你愿意让 Slowday 变得更好。

1. Fork 仓库并从 `main` 创建功能分支。
2. 保持交互轻松、克制，并兼顾键盘和触控操作。
3. 数据必须通过服务端 API 与 D1 持久化，不使用浏览器存储作为产品数据源。
4. 提交前运行 `npm test`。
5. Pull request 请说明用户问题、解决方式和验证结果；视觉改动请附截图。

安全问题请不要公开披露账号数据、访问令牌或生产环境信息。
